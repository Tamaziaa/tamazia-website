# Redirects evaluated top-to-bottom · most specific first.
#
# Phase 0 cleanup (2026-05-04):
#  · Collapsed /resources/ tree into /insights/ (institutional register canonical).
#  · Folded duplicate sitemap pointer.
#  · No em-dashes in this file (deploy gate enforces).

# Sitemap canonical alias · @astrojs/sitemap emits sitemap-index.xml + sitemap-0.xml.
/sitemap.xml  /sitemap-index.xml  200

# Phase 0 · /resources/ tree consolidates to /insights/ to remove duplicate-content dilution.
# Path-preserving so /resources/healthcare/ becomes /insights/healthcare/.
/resources/*  /insights/:splat  301

# Legacy WordPress URL retired.
/home  /  301

# Legacy aliases · both pages exist as static routes.
/privacy-policy/   /privacy-notice/  301
/privacy/          /privacy/         200
