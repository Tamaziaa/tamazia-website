# Redirects evaluated top-to-bottom · most specific first.
# (B-30 fix · /home is the lowest-priority rule — it would only fire if no other
#  /home-* route caught the request first. We have no /insights/home-* routes today,
#  but the comment is here so future authors know not to add a rule that could be
#  shadowed by /home.)

# Round-12 · @astrojs/sitemap emits sitemap-index.xml + sitemap-0.xml. Make /sitemap.xml
# transparently serve the index so search engines + crawlers find it at the canonical path.
/sitemap.xml  /sitemap-index.xml  200

# Legacy WordPress URL retired
/home  /  301

# Phase A · future route stubs (dormant until Phase G creates these pages)
/privacy-notice/   /privacy-notice/  200
/privacy-policy/   /privacy          301
/privacy/          /privacy          200
/cookie-policy/    /cookie-policy    200
